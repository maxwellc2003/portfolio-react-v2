import React from "react";
import { Helmet } from "react-helmet";

const Blog = () => {
  return (
    <main>
    <Helmet>
    <title>Maxwell Colby | Blog</title>
    </Helmet>
    </main>
  );
};

export default Blog;
